
int *a;
int *b;
float ****c;

int* func1(int p, int q);
float** func2(int a,int * b,int*** c,float *****d);

int* func1(int p,int q)
{
	int *f,*g,*a,*b;
	float *c;
	int *r;
		*a = *a + *b;
	*a = *b;
	*c = 3.2 + 5.2;
	if(!(*f == *g))
		*a = *a + *b - *f - *g;
	return f;
}


float** func2(int  q, int * r, int*** s, float***** p){
	int *a, *** b,******d;
	float **e;
	int f;
	*a = 5;
	e = func2(*a + *r,**b,b,p);
	return e;
}

void main()
{
	int *f,*g,*a,*b, *h;
	int p1 ,p2;
	int **d; int *** e; float ***** t;
	*d=&p1;
	
	if(*f > *g)
		*a = *a + *b - *f - *g;

	*b =  *f * *g / *h;
	
}


