int *d;
float *gfx, *gfy, gfz;

void main(){
        
        int x ,y, *px, *py, **ppx, **ppy;
        float *fx, *fy, fl;

        if(*px < *py && *px > *py || *px >= *py || *d <= *d){
        	fy = &fl;
        	*fx = *fx + *fy * *fy;
        }

        if(*fx > *fy && *fx > *fy || *fx <= *fy || *gfx >= *gfy ){
        	fy = &fl;
        }
}
